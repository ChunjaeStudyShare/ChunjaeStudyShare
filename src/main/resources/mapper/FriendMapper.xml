<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.fullstack7.studyShare.mapper.FriendMapper">
    <select id="list" resultType="net.fullstack7.studyShare.dto.FriendDTO">
        SELECT * FROM friend
        WHERE status = 1
        AND #{memberId} IN (friendId, requesterId)
    </select>
    <select id="searchById" resultType="java.lang.String">
        SELECT userId FROM member
        WHERE userId LIKE CONCAT('%', #{userId}, '%')
    </select>

</mapper>